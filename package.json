{
  "name": "it-audit-app",
  "version": "1.0.0",
  "private": true,
  "description": "MSP IT audit capture tool - runs locally on localhost, Node/Express/React/PostgreSQL",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "db:migrate": "npm run migrate -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
